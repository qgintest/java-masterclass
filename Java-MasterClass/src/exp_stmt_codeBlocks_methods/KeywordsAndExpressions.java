package exp_stmt_codeBlocks_methods;

public class KeywordsAndExpressions {

    public static void main(String[] args) {
        //int int = 5;   int is a reserved keyword.

        //creating an expression to convert miles into kilometers
        //below, everything right of the defined datatype is an expression
        //the operators are the = and * sign
        //the entire line is a statement
        double kilometers = (100 * 1.609344);

            //expression
        int highScore = 50;

            //expression
        if(highScore > 25){
            highScore = 1000 + highScore;
                          //expression
            //Expression////////////////
        }

             //exp
        int health = 100;

        //exp////////////////////////////////
            //exp             //exp
        if((health < 25) && (highScore > 100)){
            //exp/////////////////////////
                        //exp
            highScore = highScore - 1000;
        }
    }
}
