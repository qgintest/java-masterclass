package datatypes;

public class Math {

    public static void main(String[] args) {
        int totalInt = (Integer.MAX_VALUE / 2);
        System.out.println(totalInt);

        //comiple below because the byte when computed will become an int
        //byte totalByte = (Byte.MIN_VALUE * 2);
        //System.out.println(totalByte);

        //byte totalLiteral = (-128 * 2);
        //System.out.println(totalLiteral);


    }
}
